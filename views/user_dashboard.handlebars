<div class="grid grid-cols-1 lg:grid-cols-3 gap-4 lg:gap-0 m-5">

  <article>
    <div class="bg-white w-10/12 mt-10 rounded-lg">
      <div class="h-2 bg-indigo-400 rounded-t-md"></div>
      <div class="place-self-center">
          <div class="flex items-center justify-center pt-10 flex-col">
            <img class="rounded-full w-32"
              src="{{renderData.0.0.user.user_img}}">
            <h1 class="text-gray-800 font-semibold text-2xl mt-5">{{renderData.0.0.user.username}}</h1>
            <h1 class="text-blue-500 text- p-4">{{renderData.0.0.user.email}}</h1>
            <h1 class="text-gray-500 text-sm p-4 text-center">{{renderData.0.0.user.biography}}</h1>
          </div>
          <div class="flex items-center justify-center mt-3 mb-6 flex-col">
            <div class="flex mt-2">
              <img src="../images/facebook-16.png" alt="A facebook logo icon linked to user's facebook profile." class="w-6 border-2 p-1 rounded-full mr-3">
              <img src="../images/linkedin-3-16.png" alt="A lin logo icon linked to user's lin profile." class="w-6 border-2 p-1 rounded-full mr-3">
              <img src="../images/twitter-16.png" alt="A facebook logo icon linked to user's facebook profile." class="w-6 border-2 p-1 rounded-full mr-3">
              <img src="../images/instagram-16.png" alt="A facebook logo icon linked to user's facebook profile." class="w-6 border-2 p-1 rounded-full mr-3">
            </div>
          </div>      
        </div>
      </div>
    </article>
    

  <article class="lg:col-span-2 bg-white w-12/12 mt-10 rounded-lg pt-2 pb-6 p-3">
    <div class="h-2 bg-indigo-400 rounded-t-md"></div>
    <div class="flex items-center flex-col">
      <h1 class="font-bold mt- pb-2 border-b border-gray-200 text-5xl">Upcoming Events</h1>
    </div>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-5 gap-6">
      <card class="mt-4 p-3 w-80">
        <div class="bg-white rounded overflow-hidden shadow-md">
          <a href="{{renderData.1.0.event_url}}" target="_blank">
          <img src="{{renderData.1.0.event_image}}" alt=""/></a>
            <span class="font-bold text-lg">{{renderData.1.0.event_title}}</span>
            <span class="block text-gray-500 text-sm">{{renderData.1.0.event_start}}</span>
            <button id="btn0" class="bg-blue-400 px-2 border-2 text-white rounded container">Join Group</button>
            
        </div>
      </card>
      <card class="mt-4 p-3 w-80">
        <div class="bg-white rounded overflow-hidden shadow-md">
          <a href="{{renderData.1.1.event_url}}" target="_blank">
          <img src="{{renderData.1.1.event_image}}" alt=""/></a>
            <span class="font-bold text-lg">{{renderData.1.1.event_title}}</span>
            <span class="block text-gray-500 text-sm">{{renderData.1.1.event_start}}</span>
            <button id="btn1" class="bg-blue-400 px-2 border-2 text-white rounded container">Join Group</button>
          </div>
      </card>
      <card class="mt-4 p-3 w-80">
        <div class="bg-white rounded overflow-hidden shadow-md">
          <a href="{{renderData.1.2.event_url}}" target="_blank">
          <img src="{{renderData.1.2.event_image}}" alt=""/></a>
            <span class="font-bold text-lg">{{renderData.1.2.event_title}}</span>
            <span class="block text-gray-500 text-sm">{{renderData.1.2.event_start}}</span>
            <button id="btn2" class="bg-blue-400 px-2 border-2 text-white rounded container">Join Group</button>
        </div>
      </card>
      <card class="mt-4 p-3 w-80">
        <div class="bg-white rounded overflow-hidden shadow-md">
          <a href="{{renderData.1.3.event_url}}" target="_blank">
          <img src="{{renderData.1.3.event_image}}" alt=""/></a>
          <div class="">
            <span class="font-bold text-lg">{{renderData.1.3.event_title}}</span>
            <span class="block text-gray-500 text-sm">{{renderData.1.3.event_start}}</span>
            <button id="btn3" class="bg-blue-400 px-2 border-2 text-white rounded container">Join Group</button>
          </div>
        </div>
      </card>
      <card class="mt-4 p-3 w-80">
        <div class="bg-white rounded overflow-hidden shadow-md">
          <a href="{{renderData.1.4.event_url}}" target="_blank">
          <img src="{{renderData.1.4.event_image}}" alt=""/></a>
          <div class="">
            <span class="font-bold text-lg">{{renderData.1.4.event_title}}</span>
            <span class="block text-gray-500 text-sm">{{renderData.1.4.event_start}}</span>
            <button id="btn4" class="bg-blue-400 px-2 border-2 text-white rounded container">Join Group</button>
          </div>
        </div>
      </card>
    </div>
  </article>
</div>
      

 <script src="/javascript/group_dashboard.js"></script>

  
  <script>
    function Menu(e) {
      let list = document.querySelector('ul');

<script>
function Menu(e) {
let list = document.querySelector('ul'); e.name === 'menu' ? (e.name = "close", list.classList.add('top-[80px]'), list.classList.add('opacity-100')) : (e.name = "menu", list.classList.remove('top-[80px]'), list.classList.remove('opacity-100'))
}
</script>
<!-- END SCRIPTS -->
